# Type size: Streamlit's default (the owner tried 18 and 17 before
# settling on the default; tables keep roomier 40px rows separately).

[server]
headless = true
enableXsrfProtection = true

[browser]
gatherUsageStats = false

[client]
# never show tracebacks (which could include paths/config) to viewers
showErrorDetails = false
